import { AlertTitleDirective } from './alert-title.directive';

describe('AlertTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertTitleDirective();
    expect(directive).toBeTruthy();
  });
});
